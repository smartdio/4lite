// Shared presentation timing. These do not change projectile speed, catch
// protection windows, or scoring; confirmation runs in effective match time.
export const DODGEBALL_MOTION = Object.freeze({
  throwSeconds: .55,
  catchSeconds: .55,
  catchGatherSeconds: .12,
  catchHoldUntil: .42,
})
